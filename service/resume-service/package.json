{
  "name": "resume-service",
  "version": "1.0.0",
  "description": "This is a simple Node.js + Express backend service that reads a `resume.json` file from the `./data` directory and serves it as JSON via a REST API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
